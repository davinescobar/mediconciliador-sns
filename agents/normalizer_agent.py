"""
MedicationNormalizerAgent — role consolidated into AnalysisAgent for session 3.

normalize_medication_list() is called inside run_full_analysis(), which
AnalysisAgent invokes as a single tool. This module is a placeholder for a
future refactor that exposes normalization as a separate sub-agent step.
"""
