"""
RiskTriageAgent — role consolidated into AnalysisAgent for session 3.

score_discrepancy_risk() is called inside run_full_analysis(), which
AnalysisAgent invokes as a single tool. This module is a placeholder for a
future refactor that exposes risk triage as a separate sub-agent step.
"""
