"""
SafetyPolicyAgent — intercepts patient-facing outputs and runs policy checks.

Session 4: Implement Policy Server integration here.
CommunicationAgent currently performs an inline self-check in its instruction;
this agent will replace that with a proper policy_server.py call.
"""
